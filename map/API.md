# Описание API

Здесь описано всё API, используемое в микросервисе (используется для общения между сервисами), с описанием структур данных.

**Содержание**

1. Общее
   * 1.1. Адрес сервера
   * 1.2. Используемый протокол
2. Структуры данных
   * 2.1. Общий формат ответа
3. Список запросов
   * 3.1. Общие ошибки
4. Подробно
   * 4.1 **ЛОББИ**
   * * 4.1.1 GET_LOBBIES 
   * * 4.1.2 CREATE_LOBBY
   * * 4.1.3 JOIN_TO_LOBBY
   * * 4.1.4 LEAVE_LOBBY
   * * 4.1.5 DROP_FROM_LOBBY
   * * 4.1.6 SET_READY
   * * 4.1.7 START_GAME
   * 4.2 **КАРТА**
   * * 4.2.1 GET_RELIEF 
   * * 4.2.2 GET_VISIBILITY
   * * 4.2.3 GET_RESOURSE_VISIBILITY
   * * 4.2.4 UPDATE_UNITS
   * * 4.2.5 UPDATE_BUILDINGS



## 1. Общее

### 1.1. Адрес сервера

`http://localhost:3001`

### 1.2. Используемый протокол

API полностью реализовано на http(s).

Формат возвращаемых значений `JSON`.

Все методы, если это особо не оговорено, имеют тип `POST` и параметры передают внутри своего тела.

## 2. Структуры данных

### 2.1. Общий формат ответа

`T` - какие-то данные. В случае успешного ответа возвращается `result = 'ok'` и поле `data` с данными.

В случае ошибки возвращается `result = 'error'` и поле `error` с кодом и текстом ошибки

```
Answer<T>: {
    result: 'ok' | 'error';
    data?: T;
    error?: {
        code: number;
        text: string;
    };
}
```

### 2.2 Lobby

Здесь и всюду далее под типом данных `lobby` будет иметься ввиду следующее
```
{
    lobbyName: string; - название лобби
    lobbyGuid: string; - гуид лобби
    playersGuids: {    - гуид каждого сервиса в лобби
        spectator:       string | null,
        peopleArmy:      string | null,
        peopleEconomy:   string | null,
        mushroomArmy:    string | null,
        mushroomEconomy: string | null,
    };  
    playerIsReady: {   - готовность каждого сервиса в лобби
        spectator:       true | false,
        peopleArmy:      true | false,
        peopleEconomy:   true | false,
        mushroomArmy:    true | false,
        mushroomEconomy: true | false,
    }; 
}
```

### 2.3 Unit

Здесь и всюду далее под типом данных `unit` будет иметься ввиду следующее
```
{
    x: number,         - целочисленная координата
    y: number,         - целочисленная координата
    type: string,      - тип/название юнита
    guid: string,      - гуид юнита
    visibility: number - видимость юнита
    role: string,      - роль сервиса, отвечающего за юнита
}
```

### 2.4 Building

Здесь и всюду далее под типом данных `building` будет иметься ввиду следующее
```
{
    x: number,         - целочисленная координата
    y: number,         - целочисленная координата
    type: string,      - тип/название здания
    guid: string,      - гуид здания
    visibility: number - видимость здания
    role: string,      - роль сервиса, отвечающего за здание
    size: number       - размер здания
}
```

### 2.5 Source

Здесь и всюду далее под типом данных `source` будет иметься ввиду следующее
```
{
    x: number,          - целочисленная координата
    y: number,          - целочисленная координата
    type: string,       - тип/название здания,
    saturation: number  - объем добываемого ресурса в тик
}
```

## 3. Список запросов

| **№** | **Название** | **О чем** |
| ----- | ------------ | --------- |
| 1 | **ЛОББИ** | |
| 1.1 |  GET_LOBBIES | Получение список лобби|
| 1.2 | CREATE_LOBBY | Создание лобби | 
| 1.3 | JOIN_TO_LOBBY | Присоедининение к лобби |
| 1.4 | LEAVE_LOBBY | Покинуть текущее лобби |
| 1.5 | DROP_FROM_LOBBY | Выгнать игрока из лобби |
| 1.6 | SET_READY | Установка готовность |
| 1.7 | START_GAME | Начать игру |
| 2 | **КАРТА** |  |
| 2.1 | GET_RELIEF | Получить проходимость |
| 2.2 | GET_VISIBILITY | Получить видимость |
| 2.3 | GET_RESOURSE_VISIBILITY | Получить видимые ресурсы |
| 2.4 | UPDATE_UNITS | Обновить юнитов |
| 2.5 | UPDATE_BUILDINGS | Обновить здания |


### 3.1. Общие ошибки

* `242` - не переданы все необходимые параметры
* `404` - если данного `method` не существует
* `9000` - неизвестная ошибка

## 4. Подробно

### 4.1.1 GET_LOBBIES

Получение списка уже существующих лобби

**Параметры**

```
{
    guid: string; - гуид пользователя
}
```

**Успешный ответ**

```
Answer<[lobby]>
```


### 4.1.2 CREATE_LOBBY

Создание лобби

**Параметры**

```
{
    guid: string;      - гуид пользователя
    lobbyName: string; - название лобби
    role: string;      - роль отправителя 
}
```

**Успешный ответ**

```
Answer<lobby>
```

### 4.1.3 JOIN_TO_LOBBY

Присоединение к лобби

**Параметры**

```
{
    guid: string;      - гуид пользователя
    lobbyGuid: string; - гуид лобби
    role: string;      - роль отправителя 
}
```

**Успешный ответ**

```
Answer<lobby>
```

**Ошибки**

* `2003` - указанного лобби не существует
* `2004` - лобби заполнено
* `2005` - пользователь уже в этом лобби
* `2017` - роль пользователя уже занята

### 4.1.4 LEAVE_LOBBY

Покинуть текущее лобби

**Параметры**

```
{
    guid: string; - гуид пользователя 
}
```

**Успешный ответ**

```
Answer<true>
```

**Ошибки**

* `2006` - Вы не в комнате

### 4.1.5 DROP_FROM_LOBBY

Покинуть текущее лобби

**Параметры**

```
{
    guid: string; - гуид пользователя,
    targetGuid: string; - гуид пользователя, которого выкидывают 
}
```

**Успешный ответ**

```
Answer<lobby>
```

**Ошибки**

* `2006` - Вы не в комнате
* `2007` - Нельзя выгнать создателя комнаты
* `2009` - Игрок не в этой комнате
* `2010` - Игрок не владелец комнаты

### 4.1.6 SET_READY

Указать готовность

**Параметры**

```
{
    guid: string; - гуид пользователя
}
```

**Успешный ответ**

```
Answer<true>
```

**Ошибки**

* `1001` - Пользователя не существует
* `2006` - Вы не в комнате


### 4.1.7 START_GAME

Начать игру

**Параметры**

```
{
    guid: string; - гуид пользователя
}
```

**Успешный ответ**

```
Answer<true>
```

**Ошибки**

* `2006` - Вы не в комнате
* `2010` - Игрок не владелец комнаты
* `2012` - Не все игроки готовы

### 4.2.1 GET_RELIEF

Получить проходимость карты

**Параметры**

```
{ 
    mapGuid: string, - гуид карты
    userGuid: string - гуид пользователя
}
```

**Успешный ответ**

```
Answer<{map:[[0 | 1 | 2]]}> - Проходимость всей карты
```

**Ошибки**

* `3002` - Карты нет
* `3003` - Игрока нет на карте

### 4.2.2 GET_VISIBILITY

Получить видимость своих объектов

**Параметры**

```
{ 
    mapGuid: string, - гуид карты
    userGuid: string - гуид пользователя
}
```

**Успешный ответ**

```
Answer<
{
    units:[unit],
    buildings:[building]
}
>
```

**Ошибки**

* `3002` - Карты нет
* `3003` - Игрока нет на карте
* `3004` - Наблюдатель использует методы сервисов

### 4.2.3 GET_RESOURSE_VISIBILITY

Получить видимые ресурсов

**Параметры**

```
{ 
    mapGuid: string, - гуид карты
    userGuid: string - гуид пользователя
}
```

**Успешный ответ**

```
Answer<
{
    sourсes: [sourсe]
}
>
```

**Ошибки**

* `3002` - Карты нет
* `3003` - Игрока нет на карте
* `3004` - Наблюдатель использует методы сервисов

### 4.2.4 UPDATE_UNITS

Обновить определенных юнитов на карте. 
- При отсутствии гуида на карте - добавление его на карту
- При наличие гуида и изменении положение - изменение
- При наличие гуида и неизменном положении - удаление

**Параметры**

```
{ 
    mapGuid: string, - гуид карты
    userGuid: string - гуид пользователя
    entities: [
        {
            x: number,
            y: number,
            type: string,
            guid: string,
            visibility: number,
            sourcesVisibility: number  - опциональный параметр для получения видмости ресурсов
        }
    ]
}
```

**Успешный ответ**

```
Answer<true>
```

**Ошибки**

* `3002` - Карты нет
* `3003` - Игрока нет на карте
* `3004` - Наблюдатель использует методы сервисов

### 4.2.5 UPDATE_BUILDINGS

Обновить определенные здания на карте. 
- При отсутствии гуида на карте - добавление его на карту
- При наличие гуида - удаление

**Параметры**

```
{ 
    mapGuid: string, - гуид карты
    userGuid: string - гуид пользователя
    entities: [
        {
            x: number,
            y: number,
            type: string,
            guid: string,
            visibility: number,
            size: number
        }
    ]
}
```

**Успешный ответ**

```
Answer<true>
```

**Ошибки**

* `3002` - Карты нет
* `3003` - Игрока нет на карте
* `3004` - Наблюдатель использует методы сервисов